# Lyeria Client

React + Vite UI for collaborative control and real-time audio visualization.

## Run locally

```bash
cd client
npm install
npm run dev
```

The dev server proxies `/api` and `/ws` to `http://localhost:8000`.

## Test

```bash
cd client
npm test
```
